# 구버전 SQLite PVC 보관용. PostgreSQL 배포에는 적용하지 않습니다.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: healthcheck-monitor-state
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
